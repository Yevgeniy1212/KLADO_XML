
Процедура ОбработкаПроведения(Отказ, Режим)
	
	Движения.УчетВремениПерсонала.Записывать = Истина;
	Для Каждого ТекСтрокаВремя Из Время Цикл
		Движение = Движения.УчетВремениПерсонала.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Должность = ТекСтрокаВремя.Должность;
		Движение.Месяц = ТекСтрокаВремя.Месяц;
		Движение.КоличествоЧасов = ТекСтрокаВремя.КоличествоЧасов;
	КонецЦикла;

КонецПроцедуры
